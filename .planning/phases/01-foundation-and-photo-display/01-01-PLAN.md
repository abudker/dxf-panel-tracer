---
phase: 01-foundation-and-photo-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/types/index.ts
  - src/utils/coordinates.ts
  - src/store/useAppStore.ts
  - src/components/Toolbar.tsx
  - src/components/UploadButton.tsx
autonomous: true
requirements:
  - PHOTO-01
must_haves:
  truths:
    - "Vite dev server starts without errors"
    - "App renders a full-viewport dark background with a floating toolbar in the top-left"
    - "Toolbar contains an Open Photo button with a Lucide upload icon"
    - "TypeScript types, Zustand store, and coordinate utilities are exported and importable"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Viewport and Point type definitions"
      exports: ["Viewport", "Point"]
    - path: "src/utils/coordinates.ts"
      provides: "screenToWorld and worldToScreen coordinate transforms"
      exports: ["screenToWorld", "worldToScreen"]
    - path: "src/store/useAppStore.ts"
      provides: "Zustand store with photoUrl, viewport, setPhotoUrl, setViewport"
      exports: ["useAppStore"]
    - path: "src/components/Toolbar.tsx"
      provides: "Floating toolbar component"
      exports: ["Toolbar"]
    - path: "src/components/UploadButton.tsx"
      provides: "Open Photo button triggering file picker"
      exports: ["UploadButton"]
  key_links:
    - from: "src/store/useAppStore.ts"
      to: "src/types/index.ts"
      via: "imports Viewport type"
      pattern: "import.*Viewport.*from.*types"
    - from: "src/utils/coordinates.ts"
      to: "src/types/index.ts"
      via: "imports Viewport type"
      pattern: "import.*Viewport.*from.*types"
    - from: "src/App.tsx"
      to: "src/components/Toolbar.tsx"
      via: "renders Toolbar component"
      pattern: "<Toolbar"
---

<objective>
Scaffold the Vite + React + TypeScript project, install all Phase 1 dependencies, establish the type system, Zustand store, coordinate utilities, and app shell with floating toolbar.

Purpose: Create the foundation that all subsequent plans and phases build on. The type definitions and store shape established here are the contracts for photo display, pan/zoom, calibration, and drawing tools.

Output: A running Vite dev server showing a full-viewport dark app shell with a floating toolbar containing an "Open Photo" button.
</objective>

<execution_context>
@/Users/abudker/Projects/dxf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abudker/Projects/dxf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-photo-display/01-CONTEXT.md
@.planning/phases/01-foundation-and-photo-display/01-RESEARCH.md
@.planning/phases/01-foundation-and-photo-display/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project and install dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/index.css, src/vite-env.d.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-photo-display/01-RESEARCH.md (lines 112-117 for exact install commands, lines 453-467 for vite.config.ts and index.css patterns)
    .planning/phases/01-foundation-and-photo-display/01-UI-SPEC.md (lines 20-26 for design system: no shadcn, Lucide icons, system font stack)
  </read_first>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` to scaffold the project in the current directory. If the command prompts because the directory is non-empty, use the appropriate flag or answer to proceed (the `.planning/` and `.claude/` directories are fine to keep).

    2. Install runtime dependencies:
       ```
       npm install konva react-konva use-image zustand exifr lucide-react
       ```

    3. Install Tailwind CSS v4 with the Vite plugin:
       ```
       npm install tailwindcss @tailwindcss/vite
       ```

    4. Update `vite.config.ts` to include both the React and Tailwind plugins:
       ```typescript
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';
       import tailwindcss from '@tailwindcss/vite';

       export default defineConfig({
         plugins: [react(), tailwindcss()],
       });
       ```

    5. Replace `src/index.css` contents with ONLY:
       ```css
       @import "tailwindcss";

       html, body, #root {
         margin: 0;
         padding: 0;
         width: 100%;
         height: 100%;
         overflow: hidden;
         background-color: #1a1a1a;
         font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
       }
       ```
       IMPORTANT: Do NOT use `@tailwind base; @tailwind components; @tailwind utilities;` — those are Tailwind v3 patterns. Tailwind v4 uses `@import "tailwindcss"` only. Do NOT create a `tailwind.config.js` file.

    6. Update `src/main.tsx` to import `./index.css` and render `<App />`:
       ```typescript
       import { StrictMode } from 'react';
       import { createRoot } from 'react-dom/client';
       import './index.css';
       import App from './App';

       createRoot(document.getElementById('root')!).render(
         <StrictMode>
           <App />
         </StrictMode>,
       );
       ```

    7. Delete any Vite-generated boilerplate files that are not needed: `src/App.css`, `src/assets/react.svg`, `public/vite.svg`. Keep `src/vite-env.d.ts`.

    8. Verify the dev server starts: `npm run dev` should compile without errors.
  </action>
  <verify>
    <automated>cd /Users/abudker/Projects/dxf && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "konva" in dependencies
    - package.json contains "react-konva" in dependencies
    - package.json contains "use-image" in dependencies
    - package.json contains "zustand" in dependencies
    - package.json contains "exifr" in dependencies
    - package.json contains "lucide-react" in dependencies
    - package.json contains "tailwindcss" in dependencies
    - package.json contains "@tailwindcss/vite" in dependencies
    - vite.config.ts contains "tailwindcss()" (not postcss pattern)
    - src/index.css contains '@import "tailwindcss"'
    - src/index.css does NOT contain "@tailwind"
    - No file named tailwind.config.js exists
    - No file named tailwind.config.ts exists
    - src/App.css does NOT exist (deleted boilerplate)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Vite project scaffolded with all Phase 1 dependencies installed. Build completes without errors. Tailwind v4 configured correctly with Vite plugin (no v3 patterns).</done>
</task>

<task type="auto">
  <name>Task 2: Create type definitions, Zustand store, and coordinate utilities</name>
  <files>src/types/index.ts, src/store/useAppStore.ts, src/utils/coordinates.ts</files>
  <read_first>
    .planning/phases/01-foundation-and-photo-display/01-RESEARCH.md (lines 299-351 for world coordinate helpers and Zustand store shape)
    src/vite-env.d.ts (confirm TypeScript env is intact after scaffold)
  </read_first>
  <action>
    1. Create `src/types/index.ts` with the shared type definitions:
       ```typescript
       /** A 2D point in either screen or world coordinate space */
       export interface Point {
         x: number;
         y: number;
       }

       /** The current viewport transform: pan offset (x, y) and uniform scale */
       export interface Viewport {
         scale: number;
         x: number;  // stage offset in screen pixels
         y: number;  // stage offset in screen pixels
       }

       /** Tool modes — Phase 1 only has 'select', future phases add 'line', 'arc', 'calibrate' */
       export type ToolMode = 'select';
       ```

    2. Create `src/store/useAppStore.ts` with the Zustand store:
       ```typescript
       import { create } from 'zustand';
       import type { Viewport, ToolMode } from '../types';

       interface AppState {
         // Photo
         photoUrl: string | null;
         photoSize: { width: number; height: number } | null;
         setPhotoUrl: (url: string) => void;
         setPhotoSize: (size: { width: number; height: number }) => void;
         clearPhoto: () => void;

         // Viewport (pan/zoom)
         viewport: Viewport;
         setViewport: (v: Partial<Viewport>) => void;

         // Tool mode
         toolMode: ToolMode;
         setToolMode: (mode: ToolMode) => void;
       }

       export const useAppStore = create<AppState>((set) => ({
         photoUrl: null,
         photoSize: null,
         setPhotoUrl: (url) => set({ photoUrl: url }),
         setPhotoSize: (size) => set({ photoSize: size }),
         clearPhoto: () => set({ photoUrl: null, photoSize: null }),

         viewport: { scale: 1, x: 0, y: 0 },
         setViewport: (v) =>
           set((s) => ({ viewport: { ...s.viewport, ...v } })),

         toolMode: 'select',
         setToolMode: (mode) => set({ toolMode: mode }),
       }));
       ```

    3. Create `src/utils/coordinates.ts` with the coordinate transform helpers:
       ```typescript
       import type { Point, Viewport } from '../types';

       /**
        * Convert a screen-pixel position to world (stage-local) coordinates.
        * Accounts for current pan offset and zoom scale.
        */
       export function screenToWorld(screen: Point, v: Viewport): Point {
         return {
           x: (screen.x - v.x) / v.scale,
           y: (screen.y - v.y) / v.scale,
         };
       }

       /**
        * Convert a world (stage-local) position back to screen pixels.
        */
       export function worldToScreen(world: Point, v: Viewport): Point {
         return {
           x: world.x * v.scale + v.x,
           y: world.y * v.scale + v.y,
         };
       }

       /**
        * Calculate contain-fit: scale and position to fit an image entirely
        * within a viewport with centering.
        */
       export function calculateContainFit(
         imgW: number,
         imgH: number,
         viewW: number,
         viewH: number
       ): Viewport {
         const scale = Math.min(viewW / imgW, viewH / imgH);
         const x = (viewW - imgW * scale) / 2;
         const y = (viewH - imgH * scale) / 2;
         return { scale, x, y };
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/abudker/Projects/dxf && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts contains "export interface Point"
    - src/types/index.ts contains "export interface Viewport"
    - src/types/index.ts contains "export type ToolMode"
    - src/store/useAppStore.ts contains "import { create } from 'zustand'"
    - src/store/useAppStore.ts contains "export const useAppStore"
    - src/store/useAppStore.ts contains "photoUrl: string | null"
    - src/store/useAppStore.ts contains "viewport: Viewport"
    - src/store/useAppStore.ts contains "setViewport"
    - src/store/useAppStore.ts contains "setPhotoUrl"
    - src/utils/coordinates.ts contains "export function screenToWorld"
    - src/utils/coordinates.ts contains "export function worldToScreen"
    - src/utils/coordinates.ts contains "export function calculateContainFit"
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Type system, state store, and coordinate utilities are created and type-check cleanly. Viewport, Point, screenToWorld, worldToScreen, calculateContainFit, and useAppStore are all exported and importable.</done>
</task>

<task type="auto">
  <name>Task 3: Create app shell with floating toolbar and upload button</name>
  <files>src/App.tsx, src/components/Toolbar.tsx, src/components/UploadButton.tsx</files>
  <read_first>
    .planning/phases/01-foundation-and-photo-display/01-UI-SPEC.md (full file — color values, spacing, typography, toolbar position, button states, copywriting)
    .planning/phases/01-foundation-and-photo-display/01-CONTEXT.md (lines 13-21 for layout decisions)
    src/store/useAppStore.ts (to understand store shape for wiring)
    src/types/index.ts (to understand available types)
  </read_first>
  <action>
    1. Create `src/components/UploadButton.tsx`:
       ```typescript
       import { useRef } from 'react';
       import { Upload } from 'lucide-react';

       interface UploadButtonProps {
         onFileSelect: (file: File) => void;
       }

       export function UploadButton({ onFileSelect }: UploadButtonProps) {
         const inputRef = useRef<HTMLInputElement>(null);

         const handleClick = () => inputRef.current?.click();

         const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
           const file = e.target.files?.[0];
           if (file) {
             onFileSelect(file);
             // Reset input so same file can be re-selected
             e.target.value = '';
           }
         };

         return (
           <>
             <button
               onClick={handleClick}
               className="flex items-center gap-[4px] px-[8px] py-[8px] rounded text-[14px] font-normal leading-[1.5] text-[#f5f5f5] bg-transparent hover:bg-[#3a3a3a] active:bg-[#3b82f6] active:text-white transition-colors cursor-pointer"
               style={{ minWidth: '44px', minHeight: '44px' }}
               title="Open Photo"
             >
               <Upload size={18} className="text-[#a3a3a3]" />
               <span>Open Photo</span>
             </button>
             <input
               ref={inputRef}
               type="file"
               accept="image/jpeg,image/png,image/webp"
               onChange={handleChange}
               className="hidden"
             />
           </>
         );
       }
       ```

       Button styling per UI-SPEC:
       - Idle: bg transparent, text #f5f5f5, icon #a3a3a3
       - Hover: bg #3a3a3a
       - Active: bg #3b82f6, text white
       - Min touch target: 44px x 44px
       - Icon-to-label gap: 4px (xs spacing token)
       - Internal padding: 8px (sm spacing token)
       - Font: 14px / weight 400 / line-height 1.5

    2. Create `src/components/Toolbar.tsx`:
       ```typescript
       import { UploadButton } from './UploadButton';

       interface ToolbarProps {
         onFileSelect: (file: File) => void;
       }

       export function Toolbar({ onFileSelect }: ToolbarProps) {
         return (
           <div
             className="fixed top-[16px] left-[16px] z-50 flex items-center bg-[#2a2a2a] border border-[#3a3a3a] rounded-lg px-[8px] py-[4px]"
           >
             <UploadButton onFileSelect={onFileSelect} />
           </div>
         );
       }
       ```

       Toolbar per UI-SPEC:
       - Position: fixed, 16px from top, 16px from left
       - Background: #2a2a2a with 1px #3a3a3a border, 8px border-radius (rounded-lg)
       - Opacity: 1.0 (not translucent)
       - z-index: above canvas (z-50)

    3. Update `src/App.tsx` to render the app shell:
       ```typescript
       import { Toolbar } from './components/Toolbar';

       function App() {
         const handleFileSelect = (file: File) => {
           // Photo handling wired in Plan 02
           console.log('File selected:', file.name);
         };

         return (
           <div className="w-screen h-screen bg-[#1a1a1a] relative overflow-hidden">
             <Toolbar onFileSelect={handleFileSelect} />
             {/* CanvasStage and DropZoneOverlay added in Plan 02 */}
           </div>
         );
       }

       export default App;
       ```

       App shell per UI-SPEC and CONTEXT.md:
       - Full-viewport: w-screen h-screen
       - Background: #1a1a1a (dominant color)
       - overflow-hidden to prevent scroll bars
       - Toolbar is positioned absolutely via its own fixed positioning
  </action>
  <verify>
    <automated>cd /Users/abudker/Projects/dxf && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/UploadButton.tsx contains 'export function UploadButton'
    - src/components/UploadButton.tsx contains "import { Upload } from 'lucide-react'"
    - src/components/UploadButton.tsx contains 'accept="image/jpeg,image/png,image/webp"'
    - src/components/UploadButton.tsx contains "Open Photo"
    - src/components/UploadButton.tsx contains 'minHeight: .44px.'
    - src/components/Toolbar.tsx contains 'export function Toolbar'
    - src/components/Toolbar.tsx contains 'fixed top-[16px] left-[16px]'
    - src/components/Toolbar.tsx contains 'bg-[#2a2a2a]'
    - src/components/Toolbar.tsx contains 'border-[#3a3a3a]'
    - src/App.tsx contains '<Toolbar'
    - src/App.tsx contains 'bg-[#1a1a1a]'
    - src/App.tsx contains 'w-screen h-screen'
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>App shell renders with full-viewport dark background (#1a1a1a). Floating toolbar at top-left (16px offset) with dark gray background (#2a2a2a) and border (#3a3a3a). "Open Photo" button with Lucide Upload icon triggers file picker accepting JPG/PNG/WebP. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx tsc --noEmit` reports no type errors
3. All type exports exist: Point, Viewport, ToolMode from src/types/index.ts
4. All store exports exist: useAppStore from src/store/useAppStore.ts
5. All utility exports exist: screenToWorld, worldToScreen, calculateContainFit from src/utils/coordinates.ts
6. All component exports exist: Toolbar, UploadButton
7. No Tailwind v3 patterns present (no tailwind.config.js, no @tailwind directives)
</verification>

<success_criteria>
- Vite dev server starts and the app renders in a browser
- Full-viewport dark background (#1a1a1a) with floating toolbar top-left
- Toolbar contains "Open Photo" button with Lucide Upload icon
- Clicking "Open Photo" opens native file picker filtered to image/jpeg, image/png, image/webp
- TypeScript compiles cleanly — all types, store, and utilities are correctly typed
- Project is ready for Plan 02 to add photo display, EXIF correction, and pan/zoom
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-photo-display/01-01-SUMMARY.md`
</output>
